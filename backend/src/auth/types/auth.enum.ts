export enum Role {
  Student = 'student',
  Faculty = 'faculty',
  Admin = 'admin',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum InstituteType {
  Government = 'government',
  Private = 'private',
  Autonomous = 'autonomous',
}
